<!-- this is payment.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Options UI</title>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
  <style>
    /* Basic Reset */
    #qrcode {
            display: flex;
            justify-content: center;
            margin: 20px 0;
        }
        
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    /* Container Styling */
    .container {
      width: 350px;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      font-size: 14px;
    }

    /* Section Title */
    .section-title {
      font-weight: bold;
      color: #555;
      font-size: 14px;
      margin-bottom: 15px;
      text-transform: uppercase;
    }

    /* Header */
    .header {
      font-weight: bold;
      font-size: 16px;
      margin-bottom: 15px;
      color: #333;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header .offers {
      color: #4caf50;
      font-weight: bold;
      font-size: 14px;
    }

    /* Payment Option Buttons */
    .option {
      display: flex;
      align-items: center;
      padding: 15px;
      border: 1px solid #eee;
      border-radius: 5px;
      margin-bottom: 10px;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .option:hover {
      background-color: #f0f0f0;
    }

    .option img {
      width: 50px;
      height: 50px;
      margin-right: 10px;
      border-radius: 50%;
    }

    .option-text {
      flex-grow: 1;
      font-weight: bold;
      color: #333;
    }

    /* Modal Styling */
    .modal {
      display: none; /* Hidden by default */
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: auto;
      background-color: rgba(0, 0, 0, 0.4);
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background-color: white;
      border-radius: 8px;
      padding: 20px;
      text-align: center;
      width: 300px;
    }

    /* Loader Animation */
    .loader {
      border: 8px solid #f3f3f3; /* Light grey */
      border-top: 8px solid #3498db; /* Blue */
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: 20px auto;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    /* Countdown Timer */
    .countdown {
      margin-top: 10px;
      font-size: 16px;
      font-weight: bold;
      color: #333;
    }
  </style>
</head>
<body>
  <% if (tempUser) { %>
    <div class="container">
      <div class="section-title">UPI (Pay via any App)</div><br>
  
      <div class="header">
        <span>UPI Options</span>
        <!-- <span class="offers">3 Offers</span> -->
      </div>
  
      <!-- Payment Options as Buttons -->
      <!-- <div class="option" onclick="payViaQRPay()">
        <img src="/qrcode.png" alt="Google Pay">
        <span class="option-text">Use QR To Pay</span>
      </div>
  
      <div class="option" onclick="payViaGooglePay()">
        <img src="/img/image1.png" alt="Google Pay">
        <span class="option-text">Google Pay</span>
      </div>
  
      <div class="option" onclick="payViaPaytm()">
        <img src="/img/image2.png" alt="Paytm">
        <span class="option-text">Paytm</span>
      </div>
  
      <div class="option" onclick="payViaPhonePe()">
        <img src="/img/image3.png" alt="PhonePe">
        <span class="option-text">PhonePe </span>
      </div>
  
      <div class="option" onclick="enterUpiId()">
        <img src="/img/image4.png" alt="Enter UPI ID">
        <span class="option-text">Enter UPI ID</span>
      </div> -->
      <div class="option" >
        <img src="/img/image4.png" alt="Enter UPI ID">
        <div><a class="autoPay" style=" width: 135px; background-color: #1065b7; text-align: center; font-weight: 800; padding: 11px 0px; color: white; font-size: 12px; display: inline-block; text-decoration: none; border-radius:3.229px; " href='https://test.payumoney.com/url/BInlLLvUMnO4' > Pay Now </a> </div>
      </div>
    </div>
  
    <!-- Modal for Payment Processing -->
    <div id="paymentModal" class="modal">
      <div class="modal-content">
        <h2>Scan To Pay</h2><br>
        <div id="qrcode"></div><br>
        <h2>Payment in Progress</h2>
        <p>Waiting for payment confirmation...</p>
        <div class="loader"></div>
        <div class="countdown" id="countdown">0:15</div>
      </div>
    </div>
  
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        document.querySelector('.autoPay').click();
      });
    </script>
    <% } else { %>
      <script>
        window.location.href = '/auth/signup';
      </script>
    <% } %>
  
</body>
</html>